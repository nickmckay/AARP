% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_calibration_forward.R
\name{predict_forward_DL}
\alias{predict_forward_DL}
\title{Forward-model D/L for a set of precomputed calibration samples}
\usage{
predict_forward_DL(precomputed, AAR_params, dT_yr = 1500)
}
\arguments{
\item{precomputed}{List returned by \code{\link{precompute_forward_inputs}}.}

\item{AAR_params}{Named list with elements \code{Ae}, \code{Ea}, \code{R}, \code{x}.}

\item{dT_yr}{Integration timestep (yr). Default \code{1500}; coarse but
accurate here because the abiotic accumulation is linear in time within
each step (see package vignettes for a convergence check).}
}
\value{
Numeric vector of predicted D/L, one per precomputed sample.
}
\description{
Runs \code{\link{racemize_one_depth}} once per sample for a given set of
kinetic parameters, reusing the precomputed age-depth models and
temperature histories from \code{\link{precompute_forward_inputs}}.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/age_inversion.R
\name{log_prior_age}
\alias{log_prior_age}
\title{Log-prior for sample age}
\usage{
log_prior_age(log_age, range_ka = c(0.5, 3000))
}
\arguments{
\item{log_age}{Candidate value of \eqn{\log(\text{age in ka})}.}

\item{range_ka}{Numeric length-2 vector, the allowed age range (ka).
Default \code{c(0.5, 3000)}.}
}
\value{
Scalar log-prior (\code{0} inside the range, \code{-Inf} outside).
}
\description{
A weak, bounded-uniform prior on \eqn{\log(\text{age})}, wide enough to
cover the full calibration range (default \code{c(0.5, 3000)} ka) without
favouring any age within it. Because this is an ordinary Bayesian prior,
it can be tightened using independent information (e.g. a stratigraphic
age range, or a known minimum/maximum age) simply by narrowing \code{range_ka}
or swapping in an informative density -- something the original GLS
point-estimate calculator cannot easily accommodate.
}
